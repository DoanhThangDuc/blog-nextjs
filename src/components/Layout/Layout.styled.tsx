import styled from "styled-components";

export namespace sc {
  export const Layout = styled.div`
    position: absolute;
    z-index: 100;
    display: flex;
    width: 100%;
    justify-content: center;
    align-items: center;
  `;
}
